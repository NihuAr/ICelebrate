<Window x:Class="Kursachzhoska.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursachzhoska"
        mc:Ignorable="d"
        Title="iCelebrate" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Grid>
        <Border Background="{StaticResource BackgroundColor}"
                CornerRadius="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Кастомная верхняя панель -->
                <Border Grid.Row="0"
                        Background="{StaticResource SidebarColor}"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <!-- Кнопка минимизации -->
                            <Button Width="45"
                                    Height="40"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Click="MinimizeButton_Click"
                                    Style="{StaticResource IconButton}">
                                <Path Data="{StaticResource MinimizeIcon}" Fill="White" Width="12" Height="12" Stretch="Uniform"/>
                            </Button>

                            <!-- Кнопка максимизации/восстановления -->
                            <Button x:Name="MaximizeButton"
                                    Width="45"
                                    Height="40"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Click="MaximizeButton_Click"
                                    Style="{StaticResource IconButton}">
                                <Path x:Name="MaximizeIcon" Data="{StaticResource MaximizeIcon}" Fill="White" Width="12" Height="12" Stretch="Uniform"/>
                            </Button>

                            <!-- Кнопка закрытия -->
                            <Button Width="45"
                                    Height="40"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Click="CloseButton_Click"
                                    Style="{StaticResource IconButton}">
                                <Path Data="{StaticResource CloseIcon}" Fill="White" Width="12" Height="12" Stretch="Uniform"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Контент -->
                <Frame Grid.Row="1"
                       x:Name="MainFrame" 
                       NavigationUIVisibility="Hidden"/>
            </Grid>
        </Border>
    </Grid>
</Window>
